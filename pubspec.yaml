name: taskoon
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'   # ✅ match your lockfile (Dart 3)

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # your deps (left unpinned as you had them)
  location:
  bloc:
  flutter_bloc:
  equatable:
  fluttertoast:
  connectivity_plus:
  flutter_svg:
  dotted_border: ^2.1.0
  sms_autofill:
  camera:
  google_mlkit_face_detection:
  webview_flutter:
  file_picker: ^8.1.3
  mime: ^1.0.6
  url_launcher:
  get_storage:
  google_maps_flutter: ^2.6.0
  image_picker: ^1.1.2
  video_player: ^2.9.1
  geolocator: ^10.1.0
  share_plus: ^10.0.2
  google_fonts:
  google_maps_webservice:
  flutter_google_places:
  google_place:
  http:
  signalr_netcore: 
  geocoding:
  intl:

dev_dependencies:
  flutter_launcher_icons: ^0.14.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/taskoon_logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/
  fonts:
    - family: Poppins
      fonts:
        - asset: assets/Poppins-Regular.ttf
          weight: 400

# ✅ Force a version that declares an Android namespace (fixes the build)
dependency_overrides:
  google_api_headers: ^4.0.3
